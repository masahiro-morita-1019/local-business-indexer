# local-business-indexer

Google Maps × 業種で「HP未保有の企業」を抽出し、HP制作の営業先リストを作るツール。最終的には調査 → HP生成 → デプロイ → 営業文作成 → メール下書きまで一貫化する構想で、現在はMVP(調査フェーズ)から着手。

詳細な設計は `/Users/apple/.claude/plans/google-map-hp-goofy-thunder.md` 参照。

## 作業の進め方

### 計画段階・重要判断時は codex に意見を聞く

設計判断や計画の妥当性確認をするときは、`codex` CLI で別モデルにレビューを依頼する。

```bash
codex exec "ここに依頼内容(計画の要約や論点)を貼る"
```

- 計画書を承認してもらった後、または設計判断で迷ったときに実行
- 結果はユーザーに要約して提示し、採用/不採用を判断してもらう
- 1人開発でセカンドオピニオンが取りづらいため、codex を「もう一人のレビュアー」として活用する方針
